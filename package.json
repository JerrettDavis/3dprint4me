{
  "name": "3dprint4me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Accessible storefront and guided project intake for custom 3D modeling, printing, printer repair, tuning, and fabrication.",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "assets:version": "node scripts/version-assets.mjs",
    "validate": "node scripts/version-assets.mjs --check && node scripts/validate.mjs",
    "test:e2e": "python -m pytest tests/e2e -q",
    "audit": "python tests/audit/audit.py",
    "screenshots": "python scripts/capture_screenshots.py",
    "smoke:live": "node scripts/smoke-live.mjs",
    "test": "npm run validate && npm run test:unit && npm run test:e2e && npm run audit",
    "vercel-build": "npm run assets:version && npm run validate",
    "test:unit": "node --test tests/unit/*.test.mjs"
  },
  "homepage": "https://3dprint4.me",
  "keywords": [
    "3d-printing",
    "3d-modeling",
    "fabrication",
    "supabase",
    "vercel",
    "stripe",
    "accessible"
  ],
  "dependencies": {
    "@neondatabase/auth": "^0.5.0-beta",
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/blob": "^2.8.0",
    "jose": "^6.2.5"
  }
}
